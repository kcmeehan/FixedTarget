A simple class structure used to keep track of each event's centrality. It is
used during the processing of embedding data so that we can evaluate how
tracking efficiency changes as a function of occupancy.